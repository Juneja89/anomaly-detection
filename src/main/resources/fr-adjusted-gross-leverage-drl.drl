import com.oaknorth.drools.RatioValue;
import com.oaknorth.drools.Record;

//from row number: 1
rule "Row 1 fr-adjusted-gross-leverage-gdt"
	dialect "mvel"
	when
		record : Record( ratioName == "Adjusted Gross Leverage" )
	//	ratioValue :  from accumulate ( data >= 0.0 , data < 3.5) from record.ratioValues()
	then
// 		modify( record ) {
// 				// setAbsoluteAnomaly( 0 )
// 		}
end